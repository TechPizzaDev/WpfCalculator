<UserControl x:Class="Miniräknare.FormulaExpressionBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:l="clr-namespace:Miniräknare">

    <Grid HorizontalAlignment="Stretch" Margin="0,5,0,5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="7*"/>
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        
        <Image x:Name="StateImage" Grid.Column="0"
               Width="40" Height="40" VerticalAlignment="Center"/>

        <l:ExpressionBox x:Name="InputBox" Grid.Column="1" />

        <TextBox x:Name="ResultBox" Grid.Column="2" IsReadOnly="True" 
                 FontSize="24" FontFamily="Consolas"
                 Margin="5,0,5,0" VerticalAlignment="Center"
                 BorderThickness="0" HorizontalContentAlignment="Right" />
    </Grid>

</UserControl>
